using System.IO.Compression;
using System.Globalization;
using System.Dynamic;
using System;

namespace PracticaCSharp
{
    class Program
    {
        public static void Main(string[] args)
        {
        //* Algoritmo que determine el producto que más se vende en una tienda. 

            int ventas = int.Parse(Console.ReadLine());
            Console.WriteLine("Se ha vendido un total de " + ventas + " productos en el día de hoy");
            
        }            
    }
}